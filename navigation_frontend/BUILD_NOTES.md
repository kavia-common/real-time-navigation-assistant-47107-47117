# Build Notes

This project requires dependencies to be installed before building:
- react
- react-dom
- react-scripts

Run:
npm install
npm run build

If you still see "react-scripts: not found", delete node_modules and reinstall:
rm -rf node_modules package-lock.json
npm install
